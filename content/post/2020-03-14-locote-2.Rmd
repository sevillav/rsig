---
title: "Entrada 1: Objetos shape y raster en R"
author: "Victor Sevilla"
date: '2020-03-14'
output:
  html_document:
    df_print: paged
slug: locote-2
tags: []
categories: []
---

# Por: Victor Sevilla

###Objetivos:

Mostrar una manera de trabajar objetos espaciales del tipo vectorial (Shape) y raster (tiff) en R para realizar estudios agroambientales. 

Si conocen otra forma de hacerlo seria un gusto para nosotros recibir su valioso aporte y, asi colaborar con nuestra comunidad.

### Materiales y metodos:

-Emplearemos aplicaciones como R, RStudio y QGis.

-Datos de la cuenca hidrografica del rio Canoabo en Venezuela, tales como partes de agua, modelos digital de elevacion, sitios de muestreos de suelos entre otros.

-Los procedmientos que llevaremos a cabo incluyen: Creaciones de objetos R (vectores y raster), ploteo de capas, proyeccion cartografica, entre otros que a contiuacion se describen.

** INICIAMOS**

Verificacion de directotio de trabajo
```{r}
getwd()
```
*Cursivas*

**Activamos las librerias o paquetes necesarios:**
```{r}
library(raster)
```
Trabajamos con capas espaciales de SIG:
```{r}
#Creamos un objeto R de un archivo shapefile shp
perimetro <- shapefile("C:/Sevilla/Canoabo1/cuenca1.shp")
#Creamos un objeto R de un archivo formato tiff raster
DEM <- raster("C:/Sevilla/Canoabo1/MDE3.tif")
#Ploteamos juntos los objetos R recien creados (Modelo digital de elevacion y el perimetro)
plot(DEM, main="Modelo Digital de Elevacion (msnm)")
lines(perimetro)

# Si desea ver los objetos R en memoria
ls()
```

Podemos revisar la proyeccion cartografica de los objetos R creados:

Pendiente11:


<img alt = 'my new screenshot' width='700' src='/post/2020-03-14-locote-2_files/toldo.jpg' />
Figura 1.

Mostrar la proyeccion.

Cambiar la proyeccion de canoa a regeven.

Montar los puntos de suelos y ploteralo junto al dem y el perimetro.

ver los datos de suelos (los primeros 6 registros)

hacer un resumen de una caracteristica como pH.

Cambiamos el nombre del campo "Tree" por "arboles":
datos1 <- rename(Orange, arboles = Tree)


```{r}
knitr::kable(
  head(mtcars[, 1:8], 15), booktabs=TRUE,
  caption='Los primeros 15 renglones de nuestra base mtcars.')
```

